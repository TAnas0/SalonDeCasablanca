---
import { Image } from 'astro:assets';
import masterclasses from '../../assets/features/sdc-masterclasses-3-modified.webp';
---

<section class="bg-white mx-auto py-0 mb-2 md:mb-0">
  <div class="container mx-auto px-0">
    <div class="relative flex flex-col md:flex-row items-center gap-2 min-h-96 md:min-h-0">
      
      {/* Mobile background image */}
      <div 
        class="absolute inset-0 md:hidden bg-cover bg-bottom" 
        style={{ backgroundImage: `url(${masterclasses.src})` }}
      ></div>
      
      {/* Desktop image (side-by-side) */}
      <div class="hidden md:block md:w-1/2">
        <Image 
          src={masterclasses} 
          alt="Masterclasses" 
          class="rounded-lg shadow-xl w-full h-full object-cover"
          width={600}
          height={400}
        />
      </div>
  
      {/* Text content */}
      <div class="relative z-10 md:w-1/2 bg-gray-900/50 md:bg-transparent p-6 md:p-0 mt-8">
        <h2 class="hidden md:relative text-4xl md:text-5xl font-bold text-gray-900 mb-6">
          Masterclasses
        </h2>
        <p class="text-xl text-gray-50 md:text-gray-700">
          Whether a beginner trying to step into a field, or an advanced artist trying to sharpen your skills....
          <br />
          Expand your artistic horizons....
          <br />
          Fast-paced learning,
        </p>
      </div>
    </div>
  </div>
</section>
